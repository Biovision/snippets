<% content_for :meta_title, t('.title', page: current_page) %>

<article class="entries">
  <h1><%= t('.heading') %></h1>

  <%= paginate @collection %>
  <%= render partial: 'entries/list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
